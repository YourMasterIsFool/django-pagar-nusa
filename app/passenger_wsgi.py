from config.wsgi import application
# import imp
# import os
# import sys

#from config.wsgi import appliction

# sys.path.insert(0, os.path.dirname(__file__))

# wsgi = imp.load_source('wsgi', 'passenger_wsgi.py')
# application = wsgi.application

run = 100
